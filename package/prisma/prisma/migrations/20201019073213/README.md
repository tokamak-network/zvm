# Migration `20201019073213`

This migration has been generated by cd4761 at 10/19/2020, 4:32:13 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;
CREATE TABLE "new_EncryptedWallet" (
    "id" TEXT NOT NULL,
    "ciphertext" TEXT NOT NULL,
    "iv" TEXT NOT NULL,
    "algorithm" TEXT NOT NULL,
    "keylen" INTEGER NOT NULL,
    "kdf" TEXT NOT NULL,
    "N" INTEGER NOT NULL,
    "r" INTEGER NOT NULL,
    "p" INTEGER NOT NULL,
    "salt" TEXT NOT NULL,
PRIMARY KEY ("id")
);
INSERT INTO "new_EncryptedWallet" ("id", "ciphertext", "iv", "algorithm", "keylen", "kdf", "N", "r", "p", "salt") SELECT "id", "ciphertext", "iv", "algorithm", "keylen", "kdf", "N", "r", "p", "salt" FROM "EncryptedWallet";
DROP TABLE "EncryptedWallet";
ALTER TABLE "new_EncryptedWallet" RENAME TO "EncryptedWallet";
CREATE TABLE "new_Keystore" (
    "address" TEXT NOT NULL,
    "ownerId" INTEGER,
    "encrypted" TEXT,
    "balance" INTEGER,

    FOREIGN KEY ("ownerId") REFERENCES "User"("id") ON DELETE SET NULL ON UPDATE CASCADE,
PRIMARY KEY ("address")
);
INSERT INTO "new_Keystore" ("ownerId", "address", "encrypted", "balance") SELECT "ownerId", "address", "encrypted", "balance" FROM "Keystore";
DROP TABLE "Keystore";
ALTER TABLE "new_Keystore" RENAME TO "Keystore";
PRAGMA foreign_key_check;
PRAGMA foreign_keys=ON
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201019072542..20201019073213
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -18,9 +18,9 @@
 model User {
   id    Int     @id @default(autoincrement())
   name  String @unique
-  wallet EncryptedWallet[]
+  keys Keystore[]
 }
 // model Config {
@@ -28,10 +28,9 @@
 //   address String
 // }
 model Keystore {
-  id Int @id @default(autoincrement())
-  address String
+  address String @id
   owner User? @relation(fields: [ownerId], references: [id])
   ownerId Int?
   encrypted String? // stringified json
   balance Int?
```


